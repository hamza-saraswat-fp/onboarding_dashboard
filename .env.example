# Copy to .env.local and fill in. Real values are never committed.

# Read-only connection to the onboarding app's Supabase (use a read-only role).
DATABASE_URL=

# Auth.js (NextAuth v5)
# Generate a secret with: npx auth secret
AUTH_SECRET=
# Google OAuth client credentials (Google Cloud console).
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
# Local dev only: set to "true" to bypass Google SSO with a stub user.
# Ignored when NODE_ENV=production. Leave empty to use real Google SSO.
AUTH_DEV_BYPASS=
